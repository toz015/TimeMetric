% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pam.predicted_survival_eval_cr.R
\name{pam.summary_cr}
\alias{pam.summary_cr}
\title{Summarize multiple competing-risks models into a wide comparison table}
\usage{
pam.summary_cr(
  models,
  metrics = NULL,
  t_star = NULL,
  tau = NULL,
  event_type = 1
)
}
\arguments{
\item{models}{A **named list**; each element is a list with components:
\itemize{
  \item \code{pred_cif}   — numeric matrix of predicted CIF values (rows: time grid; cols: subjects)
  \item \code{time.cif}   — numeric vector of the time grid corresponding to \code{pred_cif} rows
  \item \code{event_time} — numeric vector of observed times
  \item \code{status}     — integer vector of event codes (0=censoring; \code{event_type}=target; others=competing)
}}

\item{metrics}{Character vector of metrics to compute (passed through to
\code{pam.predicted_survial_eval_cr()}); use \code{"all"} for all supported.}

\item{t_star}{Optional numeric evaluation time (passed through).}

\item{tau}{Optional numeric truncation time (passed through).}

\item{event_type}{Integer code for the cause of interest (passed through).}
}
\value{
A data.frame with:
\itemize{
  \item each row = metric
  \item each column = model name
  \item cell values = metric values
}
}
\description{
Calls \code{pam.predicted_survial_eval_cr()} for each model in a named list and
returns a wide table where rows are metrics and columns are model names.
}
