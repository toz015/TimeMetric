% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{summary_pred_plot}
\alias{summary_pred_plot}
\title{Arrange multiple prediction plots with letter tags}
\usage{
summary_pred_plot(
  data_list,
  titles = NULL,
  plot_fun = plot_pred,
  ncol = NULL,
  tag_levels = "a",
  invert_linear = TRUE,
  xlab = "Risk Score",
  ylab = "Days",
  label_name = c("censored", "event"),
  levels = NULL,
  shape_style = NULL,
  legend_name = "status"
)
}
\arguments{
\item{data_list}{List of data frames, each containing columns:
\code{linear.pred}, \code{pred}, \code{times}, and \code{status}.}

\item{titles}{Optional character vector, same length as \code{data_list};
if \code{NULL}, defaults to "Panel 1", "Panel 2", etc.}

\item{plot_fun}{Function that creates a single panel (default = \code{plot_pred}).}

\item{ncol}{Number of columns in the panel layout (default = 2).}

\item{tag_levels}{Letter style for tags: \code{"a"}, \code{"A"}, \code{"1"}, \code{"i"}, or \code{"I"}.}

\item{invert_linear}{Logical or logical vector; if length 1, recycled to all panels.}

\item{xlab, ylab}{Axis labels applied to all panels (defaults are "Risk Score" and "Days").}

\item{label_name}{Character vector of labels for status legend (e.g., \code{c("censored", "event")}).}

\item{levels}{Numeric vector for \code{status} level ordering.}

\item{shape_style}{Numeric vector of plotting symbols (pch values) for censoring/event status.}

\item{legend_name}{Character string for the legend title (default = "status").}

\item{tag_prefix, tag_suffix}{Strings for wrapping tags, e.g., "(" and ")" for "(a)", "(b)" (default).}

\item{legend_position}{Legend position for the combined plot (default = "bottom").}
}
\value{
A patchwork \code{ggplot} object combining all panels.
}
\description{
Builds one panel per data frame using \code{plot_fun} (default: \code{plot_pred})
and arranges them with letter tags. The function collects a single shared legend
and applies global styling (titles, axis labels, legend options) across panels.
}
\examples{
\dontrun{
summary_pred_plot(
  list(df1, df2),
  titles = c("Weibull AFT", "Cox PH"),
  invert_linear = c(TRUE, FALSE),
  tag_levels = "a",
  tag_prefix = "(",
  tag_suffix = ")",
  label_name = c("censored", "event"),
  shape_style = c(1, 19),
  legend_position = "bottom"
)
}
}
