% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NAMESPACE.R, R/pam.predicted_survival_eval_cr.R
\name{pam.predicted_survial_eval_cr}
\alias{pam.predicted_survial_eval_cr}
\title{Evaluate Survival Prediction Performance with Competing Risks}
\usage{
pam.predicted_survial_eval_cr(
  pred_cif,
  event_time,
  time.cif,
  status,
  metrics = "all",
  t_star = NULL,
  tau = NULL,
  event_type = 1
)

pam.predicted_survial_eval_cr(
  pred_cif,
  event_time,
  time.cif,
  status,
  metrics = "all",
  t_star = NULL,
  tau = NULL,
  event_type = 1
)
}
\arguments{
\item{pred_cif}{A matrix of predicted cumulative incidence functions (CIFs) for different subjects.}

\item{event_time}{A numeric vector of observed event or censoring times for subjects.}

\item{time.cif}{A numeric vector of time points corresponding to the predicted CIFs.}

\item{status}{A numeric vector indicating the event type for each subject:
\itemize{
  \item `0` for censored cases.
  \item `1` for the primary event of interest.
  \item `2` (or other values) for competing events.
}}

\item{metrics}{A character vector specifying the performance metrics to calculate. 
The default is `"all"`, which computes all available metrics. 
Valid options include:
\itemize{
  \item `"Pseudo_R_square"`
  \item `"R_square"`
  \item `"L_square"`
  \item `"C_index"`
  \item `"Brier Score"`
  \item `"Time Dependent Auc"`
}}

\item{t_star}{Optional numeric value specifying a reference time for evaluating 
the Brier Score and Time-Dependent AUC. Defaults to the median of `event_time`.}

\item{tau}{Optional numeric value specifying a restricted time horizon for model evaluation. 
Defaults to the maximum observed event time plus one.}

\item{event_type}{An integer indicating the primary event type of interest (default = `1`).}
}
\value{
A data frame with two columns:
  \itemize{
    \item `"Metric"`: Name of the evaluated metric.
    \item `"Value"`: Computed value of the corresponding metric.
  }

A data frame with two columns:
  \itemize{
    \item `"Metric"`: Name of the evaluated metric.
    \item `"Value"`: Computed value of the corresponding metric.
  }
}
\description{
This function evaluates the predictive performance of a survival model in the presence of competing risks.
It calculates various performance metrics including Pseudo R-squared, R-squared, L-squared, C-index, 
Brier Score, and Time-Dependent AUC.

This function evaluates the predictive performance of a survival model in the presence of competing risks.
It calculates various performance metrics including Pseudo R-squared, R-squared, L-squared, C-index, 
Brier Score, and Time-Dependent AUC.
}
\examples{
\dontrun{
result <- pam.predicted_survial_eval_cr(
    pred_cif = pred_matrix,
    event_time = event_times,
    time.cif = pred_time_points,
    status = event_status,
    metrics = c("C_index", "Brier Score"),
    tau = 5
)
print(result)
}




\dontrun{

result <- pam.predicted_survial_eval_cr(
    pred_cif = pred_matrix,
    event_time = event_times,
    time.cif = pred_time_points,
    status = event_status,
    metrics = c("C_index", "Brier Score"),
    tau = 5
)
print(result)
}




}
